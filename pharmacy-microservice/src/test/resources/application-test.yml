spring:
  application:
    name: pharmacy-microservice-test
  datasource:
    url: jdbc:h2:mem:pharmacy_db;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  flyway:
    enabled: false
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

datamate:
  authz:
    opa:
      config-path: classpath:opa-config.yaml
